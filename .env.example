# Public — injected at build time. Leave empty until GTM is configured.
PUBLIC_GTM_ID=

# Server only — where /api/lead forwards normalized leads (CRM, Zapier/Make, FUB, HubSpot…).
# Empty = dry run: the endpoint validates, scores and echoes the lead with 202.
LEAD_WEBHOOK_URL=
LEAD_WEBHOOK_TOKEN=
